# ComfyUI-ShortDramaJSON

短剧 JSON → 角色/场景绑图 + 分镜循环 + 成片拼接。

## 说明

把插件放到 `ComfyUI/custom_nodes/ComfyUI-ShortDramaJSON`，重启 ComfyUI。

本机运行目录：`D:\AI\ComfyUI_windows_portable\ComfyUI\custom_nodes\ComfyUI-ShortDramaJSON`。桌面项目有改动时须同步到该目录后再重启。

1. 在「角色场景图绑定」粘贴短剧 JSON，按角色/场景名接参考图；**角色槽可以不接图**（只当文字描述）
2. 「分镜选择/循环」设开始/结束镜；把其 `shot_prompt` 接到绑定节点入口
3. **MiniMax 的 prompt 接绑定节点输出的 `shot_prompt`**（已按本镜、已接的参考图重编号）。不要接完整 `prompt_json`
4. 时长接「分镜选择/循环」的 `duration`（例如分镜3为 3s）；单镜设 `3:3`，多镜把 SaveVideo 接到「续跑并拼接」：未完自动 Queue，最后一镜自动拼成片
5. **（可选）续接帧数**：在「分镜选择/循环」填 CSV（与分镜时长同格式），如 `0,5,0,5,5,5`——某镜为 `0` 表示该镜不续接；非 0 取上一镜末 N 帧。**成片查找前缀** 填 `ShortDrama_`。节点会输出上一镜末帧/视频/音频；**是否接到 MiniMax 或 Motion Context 由你自己拉线**。
6. **（可选）Latent 续接**：自行接线 `保存本镜 Latent` / `加载上一镜 Latent` + `H3 Motion Context`。`context_length` 仍是原版下拉时，中间接「续接帧数→Motion Context」适配节点；并把 **续接帧数** 也接到「加载上一镜 Latent」（`0` 时通传）。

示例工作流（夜晚告别，以本仓库 JSON 为准）：

| 工作流 | 说明 |
| ------ | ---- |
| `example/MIXMAX-H3 Lantent潜空间参考生成 .json` | **推荐**：末帧 I2VA + Latent Motion Context，语音连续 |
| `example/MIXMAX-H3 末尾帧参考生成.json` | 末帧 + 上一镜视频/音频参考（易口型不同步，作对照） |

短剧 JSON 与素材（`example/file/夜晚告别/`）：

| 文件 | 说明 |
| ---- | ---- |
| `短剧提示词_夜晚告别.json` | 完整字段：`衔接`/`镜头`/`构图`/`光影`/`画面`/`禁止`/`对白`/`声音` + `内容` |
| `短剧提示词_夜晚告别(自定义).json` | 精简写法：每镜只需 `编号`/`时长`/`内容`（有 `内容` 则直送 MiniMax） |
| 男主 / 女主 / 场景 / 首帧 | 参考图 |
| `Lantent潜空间参考生成.mp4` / `末尾帧参考生成.mp4` | 成片样例 |

## 节点


| 节点       | 作用                                                    |
| -------- | ----------------------------------------------------- |
| 角色场景图绑定  | 按 JSON 建槽；可接「上一镜末帧」并入末位 Picture |
| 分镜选择/循环  | 选当前镜；输出本镜续接帧数 + 上一镜末帧/视频/音频（自行决定接哪几根线） |
| 保存本镜 Latent | 按 `shot_index` 存 H3 AV latent（供 Motion Context） |
| 加载上一镜 Latent | 读上一镜 latent；第 1 镜、续接帧数=0 或缺失返回 `None`（通传） |
| 续接帧数→Motion Context | INT→原版 `context_length` COMBO（不改 H3-Motion-Context） |
| 上一镜成片   | 已并入分镜选择，可删（旧图仍可用） |
| 续跑并拼接   | 未完自动 Queue 下一镜；最后一镜按分镜名拼成片                         |


## JSON

### 自定义写法（推荐上手）

只写顶层设定 + 每镜 `编号`/`时长`/`内容`。有 `内容` 时**直接**送给 MiniMax，不再 dumping 中文分镜字段。完整 6 镜见 `example/file/夜晚告别/短剧提示词_夜晚告别(自定义).json`：

```json
{
  "整体风格": "16:9 横屏，真人实拍电影感……只保留自然眨眼、极轻呼吸与本镜写明的微表情。",
  "角色图片": {
    "男生": "东亚年轻男性，短黑发，灰棕色宽松短袖……",
    "女生": "东亚年轻女性，黑色长发，深灰针织外套……"
  },
  "场景图片": {
    "门前": "夜间门前空间：门外暗厅透过深色门框望向现代厨房……",
    "首帧": "16:9 门框双人中景静帧……subject_box ……"
  },
  "镜头规则": "优先门框双人中景、肩后硬切；对白口型同步；分镜2起引用 <上一镜末帧> 作 I2VA 首帧对齐。",
  "分镜序列": {
    "分镜1": [
      {
        "编号": 1,
        "时长": "5s",
        "内容": "For the target video, at 0.00 seconds into the target video, <首帧> (from [Shot 1]) is fully referenced.\n\nintegrated_multimodal_description: [Shot 1] ……\n\noverall_soundscape: ……\n\nnon_diegetic_music: ……"
      }
    ],
    "分镜2": [
      {
        "编号": 1,
        "时长": "5s",
        "内容": "For the target video, at 0.00 seconds into the target video, <上一镜末帧> (from [Shot 1]) is fully referenced.\n\nintegrated_multimodal_description: [Shot 1] ……<d>[Chinese] 想好了吗？</d>……\n\noverall_soundscape: ……\n\nnon_diegetic_music: ……"
      }
    ]
  }
}
```

### 完整字段写法

在自定义基础上可再补 `衔接`/`镜头`/`构图`/`光影`/`画面`/`禁止`/`对白`/`声音`（无 `内容` 时会 dumping 这些中文字段）。见 `example/file/夜晚告别/短剧提示词_夜晚告别.json`。

武侠密谋长示例仍见 `file/地主密谋杀害酒店老板/短剧提示词_地主密谋杀害酒店老板.json`。

**顶层字段**


| 字段     | 说明                         |
| ------ | -------------------------- |
| `整体风格` | 全片画风、时段、气氛总述               |
| `角色图片` | 角色名 → 外观描述；键名会显示为槽位，**可以不接参考图** |
| `场景图片` | 场景名 → 环境描述；同上，未接图则只当文字 |
| `镜头规则` | 运镜/引用/一致性约束（会拼进当前镜上下文）     |
| `分镜序列` | `分镜1`、`分镜2`… → 镜头数组（可多镜切换） |


**分镜条目**


| 字段   | 说明                                    |
| ------ | ------------------------------------- |
| `编号` | 镜内序号                                  |
| `时长` | 如 `"3s"` / `"5s"`；节点按此求和                       |
| `衔接` | 可选；与上一镜的切法/换景说明                       |
| `镜头` | 景别/切法/运镜，可用 `<角色名>`                    |
| `构图` | 可选；主体位置与景深关系                          |
| `光影` | 可选；昼夜与主光；夜间镜建议写明「首帧即夜」               |
| `画面` | 场景动作描述，引用 `<场景>` / `<角色>`             |
| `禁止` | 可选；用**肯定句**写清要保持的状态（如「只保留呼吸与眨眼」），少写一长串「不要…」 |
| `内容` | 可选；H3 官方结构提示词，有则**直接**送给 MiniMax（优先于 dumping 中文 JSON）；也可用旧键 `H3` |
| `对白` | `{ "角色名": "台词" }`，键须在 `角色图片` 中；无对白用 `{}` |
| `声音` | 环境音、动作音与口型要求                          |


**引用规则**

- 画面/镜头里用 `<名称>`，须与 `角色图片` / `场景图片` 的键一致
- 已接参考图时展开为 `<名称>/<Picture N>`；未接图只保留 `<名称>`，不占用 MiniMax 参考图序号
- 同一分镜内时段、衣着、站位与场景保持不变
- 兼容旧键名 `角色档案` / `场景档案`
- 续镜可写 `<上一镜末帧>`；绑定打开「末帧作首帧」时会写成 I2VA 首帧对齐

## 分镜

- **开始分镜 : 结束分镜**：按 JSON 里分镜的**排列顺序**取第几条到第几条（含），不是按键名里的数字。`2:2` 只跑顺序第2条，`1:5` 跑前5条
- 保存/拼接文件名用**分镜名**（如 `ShortDrama_分镜6`），不是跑批序号。JSON 可写成 `分镜1、分镜2、分镜4…`，拼接会按名找齐
- 「续跑并拼接」自动从同图「分镜选择/循环」读取 JSON；多镜只需接这一个节点
- 点「刷新分镜信息」会把结束镜同步成 JSON 总镜数

## 续接帧数

循环时「分镜选择/循环」可输出上一镜末帧/视频/音频；**接哪些口由你自己拉线**。

1. 填 **续接帧数** CSV（与分镜时长同格式）。例 `0,5,0,5,5,5`：第 3 镜为 `0`=不续接；非 0=取上一镜末 N 帧。点「刷新分镜信息」可生成默认 `0,22,22,…`（第 1 镜 0）。**成片查找前缀** 填 `ShortDrama_`（不是「保存文件名」）
2. 常用接线示例：
   - `上一镜末帧` → 绑定 → MiniMax `ref_image_*`（I2VA）
   - 需要参考视频时：`上一镜视频` → `ref_video_0`，`上一镜音频` → `ref_video_audio_0` / `ref_audio_0`（口型易不同步，优先用 Latent 续接）
   - 需要 latent 续接时：用「保存/加载本镜 Latent」+ Motion Context（见下），可不接 ref_video
3. 绑定打开 **末帧作首帧**（默认开）时，提示词会写成 I2VA 首帧对齐
4. 第 1 镜、续接帧数为 0、或找不到成片时输出为 `None`，下游跳过
5. 参考视频会压到合法帧数并缩小短边；无对白镜自动不传上一镜人声音频

### Latent 续接（可选，自行接线）

依赖 `ComfyUI-H3-Motion-Context`。

```
Sampler ──► 保存本镜 Latent (shot_index)
分镜选择 shot_index ──► 加载上一镜 Latent ──► Motion Context.context_latent
分镜选择 续接帧数 ──► 加载上一镜 Latent（0=通传）
分镜选择 续接帧数 ──►「续接帧数→Motion Context」──► Motion Context.context_length
ReferenceToVideo ──► Motion Context ──► Guider
Decode ──► Motion Context Trim ──► CreateVideo ──► SaveVideo
```

- Motion Context 保持原版下拉 `context_length`（22/5/39/56）；由适配节点对齐 CSV
- 推荐 `audio_context_length=24`
- 全片分辨率须一致（latent 不可缩放）

## 生成结果

- [Lantent潜空间参考生成.mp4](example/file/夜晚告别/Lantent潜空间参考生成.mp4)（推荐）
- [末尾帧参考生成.mp4](example/file/夜晚告别/末尾帧参考生成.mp4)
