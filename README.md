# ComfyUI-ShortDramaJSON

短剧 JSON → 角色/场景绑图 + 分镜循环 + 成片拼接。

## 说明

把插件放到 `ComfyUI/custom_nodes/ComfyUI-ShortDramaJSON`，重启 ComfyUI。

本机运行目录：`D:\AI\ComfyUI_windows_portable\ComfyUI\custom_nodes\ComfyUI-ShortDramaJSON`。桌面项目有改动时须同步到该目录后再重启。

1. 在「角色场景图绑定」粘贴短剧 JSON，按角色/场景名接参考图
2. 「分镜选择/循环」设开始/结束镜；**MiniMax 的 prompt 必须接本节点的 `shot_prompt`**（不要接绑定节点的完整 `prompt_json`）
3. 把 `shot_prompt` 再接到「角色场景图绑定」的 `shot_prompt`：本镜未引用的 `<Picture N>` 参考图会被置空，不送进 MiniMax
4. 时长接本节点的 `duration`（例如分镜3为 3s）；单镜设 `3:3`，多镜把 SaveVideo 接到「续跑并拼接」：未完自动 Queue，最后一镜自动拼成片

示例工作流见 `example/`。

## 节点


| 节点       | 作用                                                    |
| -------- | ----------------------------------------------------- |
| 角色场景图绑定  | 按 JSON 建槽；第一行 `prompt_json` 直通，中间角色/场景左右对齐，末行 **总时长** |
| 分镜选择/循环  | 选当前镜，输出提示词/时长                                         |
| 续跑并拼接   | 未完自动 Queue 下一镜；最后一镜按分镜名拼成片                         |


## JSON

```json
{
  "整体风格": "3D CG，电影感，写实数字人类，古代东方市井武侠质感；……无现代物件、建筑、服饰、灯具、文字或交通工具",
  "角色图片": {
    "杀手": "中年男子，黑发高束、垂黑发绳，面容冷峻；深蓝近黑交领短袍……",
    "地主": "中年男子，沧桑老实脸，短黑发向上梳理……",
    "店小二": "约十岁女孩，双丫髻，眼神机灵带笑……",
    "酒店老板": "中年圆脸，八字须配小山羊须；青绿花纹瓜皮帽顶红珠……"
  },
  "场景图片": {
    "春香酒楼": "石板巷上两层灰瓦木构门面；横匾金字「春香酒楼」……",
    "杀手住所": "高夯土墙后隐蔽小院；青灰小砖房……",
    "酒楼内部": "仅酒楼内景——勿露外夜街与外匾。日：左侧木格窗强暖光柱……夜：窗外墨蓝，油灯与柜台暖光主导……"
  },
  "镜头规则": "优先过肩或面部特写硬切；对白时紧跟说话人脸；……角色与场景用尖括号引用；对白口型同步；每句台词不超过五秒",
  "分镜序列": {
    "分镜1": [
      {
        "编号": 1,
        "时长": "3s",
        "衔接": "冷开场",
        "镜头": "硬切至过肩中近景；机位静止；焦点在<地主>面部与抬起酒碗的手",
        "构图": "中近景；<地主>前景右侧；桌对面<店小二>肩部虚焦",
        "光影": "日间；左侧格窗强暖光柱；浮尘",
        "画面": "日间<酒楼内部>。角落方桌。<地主>抬酒碗半遮下半脸，……低声密谋，口型同步",
        "禁止": "勿露外街；无现代道具",
        "对白": { "地主": "今夜把掌柜灌醉，亥时动手。" },
        "声音": "日间酒楼低嘈杂、轻碗沿触碰……耳语对白清晰且口型同步"
      },
      {
        "编号": 2,
        "时长": "2s",
        "衔接": "同场反打硬切",
        "镜头": "硬切反打过肩；机位静止；焦点在<店小二>面部",
        "构图": "<店小二>特写；身后柜台与酒坛虚焦",
        "光影": "同日间内景光柱",
        "画面": "仍为日间<酒楼内部>。<店小二>凑近，轻点头，……口型同步作答",
        "禁止": "勿有新角色入画",
        "对白": { "店小二": "好酒备足了，灌醉他就行。" },
        "声音": "日间酒楼环境声延续；孩童轻声带笑，口型同步"
      }
    ],
    "分镜2": [
      {
        "编号": 1,
        "时长": "3s",
        "衔接": "硬切；换景至小院",
        "镜头": "硬切至过肩特写；机位静止；焦点在<地主>面部",
        "构图": "<地主>特写；木板床与陶壶背景虚焦",
        "光影": "日间；院落斜硬日光；高反差",
        "画面": "日间<杀手住所>檐下。<地主>面向画外<杀手>，……口型同步",
        "禁止": "本单元勿用夜光",
        "对白": { "地主": "小二灌醉他，你从侧门进，下手干净点。" },
        "声音": "院落蝉鸣、轻布料摩擦；低沉清晰对白，口型同步"
      },
      {
        "编号": 2,
        "时长": "2s",
        "衔接": "同院反打硬切",
        "镜头": "硬切反打特写；机位静止；焦点在<杀手>面部与按在刀鞘上的拇指",
        "构图": "<杀手>特写；拇指按鞘可见",
        "光影": "同日间硬光",
        "画面": "仍为日间<杀手住所>。<杀手>微微颔首；拇指按住黑刀鞘……",
        "禁止": "尚未拔刀",
        "对白": { "杀手": "收到。" },
        "声音": "日间院落环境声延续、轻刀鞘轻叩；低沉男声，口型同步"
      }
    ]
  }
}
```

完整示例见 `file/短剧提示词_地主密谋杀害酒店老板.json`（含分镜3–7：外景日转夜、夜间醉倒、轻推侧门、潜入、出鞘前一瞬）。

**顶层字段**


| 字段     | 说明                         |
| ------ | -------------------------- |
| `整体风格` | 全片画风、时段、气氛总述               |
| `角色图片` | 角色名 → 外观描述；键名即槽位名，有几个显示几个  |
| `场景图片` | 场景名 → 环境描述；同上              |
| `镜头规则` | 运镜/引用/一致性约束（会拼进当前镜上下文）     |
| `分镜序列` | `分镜1`、`分镜2`… → 镜头数组（可多镜切换） |


**分镜条目**


| 字段   | 说明                                    |
| ---- | ------------------------------------- |
| `编号` | 镜内序号                                  |
| `时长` | 如 `"3s"`；节点按此求和                       |
| `衔接` | 可选；与上一镜的切法/换景说明                       |
| `镜头` | 景别/切法/运镜，可用 `<角色名>`                    |
| `构图` | 可选；主体位置与景深关系                          |
| `光影` | 可选；昼夜与主光；夜间镜建议写明「首帧即夜」               |
| `画面` | 场景动作描述，引用 `<场景>` / `<角色>`             |
| `禁止` | 可选；明确禁止的动作/光影（如上桌走动、镜内日转夜）            |
| `对白` | `{ "角色名": "台词" }`，键须在 `角色图片` 中；无对白用 `{}` |
| `声音` | 环境音、动作音与口型要求                          |


**引用规则**

- 画面/镜头里用 `<名称>`，须与 `角色图片` / `场景图片` 的键一致
- 生成时展开为 `<名称>/<Picture N>`
- 同一分镜内时段、衣着、站位与场景保持不变
- 兼容旧键名 `角色档案` / `场景档案`

## 分镜

- **开始分镜 : 结束分镜**：按 JSON 里分镜的**排列顺序**取第几条到第几条（含），不是按键名里的数字。`2:2` 只跑顺序第2条，`1:5` 跑前5条
- 保存/拼接文件名用**分镜名**（如 `ShortDrama_分镜6`），不是跑批序号。JSON 可写成 `分镜1、分镜2、分镜4…`，拼接会按名找齐
- 「续跑并拼接」自动从同图「分镜选择/循环」读取 JSON；多镜只需接这一个节点
- 点「刷新分镜信息」会把结束镜同步成 JSON 总镜数

## 例图

**单个分镜生成**

<img src="file/1.png" alt="单个分镜生成" width="100%" />

**多场景循环生成**

<img src="file/2.png" alt="多场景循环生成" width="100%" />

**生成结果**

<video src="file/生成结果.mp4" controls width="100%"></video>

若预览无法播放，直接打开：[生成结果.mp4](file/生成结果.mp4)